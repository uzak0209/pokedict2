name: CI (Disabled)

# This workflow is disabled because the backend Rust project has been
# replaced by backend-hono (TypeScript). Linting and type-checking
# for backend-hono is handled by deploy.yml.
#
# To re-enable, restore the 'on:' trigger block.

on:
  workflow_dispatch: # manual trigger only

jobs:
  noop:
    name: No-op (CI disabled)
    runs-on: ubuntu-latest
    steps:
      - run: echo "CI workflow is disabled. See deploy.yml for backend-hono checks."
